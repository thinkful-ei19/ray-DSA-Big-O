function findRandomElement(arr) {
    return arr[Math.floor(Math.random() * arr.length)];
}

// O(1) Constant Time - this algorithm will just return the input array after running through one operation.